"use strict"; //treat all JS code as newer version

/* alert("hello")
it's an brower method and can't used in node.js */

console.log("Madhu") //code readability must be high

let name = "Madhu" //string most of the times used inside " " and sometimes ' ' 
let age = 20  //number range 2^53, beyound that bigInt is used
let isLogged = true //booleean true/false

// null => datatype along with it ,it's an standalone Value also

// undefined => means value is not assigned yet 

// symbol => used to find uniqueness ,mostly used in react and figma 

//object 

console.log(typeof name);
console.log(typeof null); //type => object
console.log(typeof undefined);// type => undefined
